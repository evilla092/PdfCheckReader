<h2>You are currently logged in as <%= current_user.email %> </h4> 

<%= link_to "Sign Out", destroy_user_session_path, data: { "turbo-method": :delete } %>

<br>
<br>

<%= link_to "Add Employer", new_employer_path %>

<h4><%= link_to 'Employers', employers_path %><h4>
<ul>
<% Employer.all.each do |employer| %>
<li> <%= employer.name %> </li>
<% end %>
</ul>
