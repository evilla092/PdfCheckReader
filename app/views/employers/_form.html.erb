<%= form_with(model: employer) do |form| %>
  <% if employer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(employer.errors.count, "error") %> prohibited this employer from being saved:</h2>

      <ul>
        <% employer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :dues_rate, style: "display: block" %>
    <%= form.text_field :dues_rate %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: none" %>
    <%= form.text_field :user_id, value: current_user.id, hidden:true %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
